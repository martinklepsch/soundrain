(ns scripper.handler
  (:use compojure.core scripper.view)
  (:require [compojure.handler :as handler]
            [compojure.route :as route]))

(defroutes app-routes
  (GET "/" [] (url-form))
  (POST "/" [] (results (params :url)))
  (route/not-found "Not Found"))

(def app
  (handler/site app-routes))
