// Can create and decode ID3 tags (when it's finished)
var ID3 = {
  // data is an array of dictionaries which follow this pattern:
  //    {type: "<ID3 frame identifier", data: "<data as binary string>"}
  createTag: function(data, audio) {
    for (var i=0; i<data.length, i++) {

    }
  }
}